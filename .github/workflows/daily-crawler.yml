name: Daily crawler

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  discovery:
    uses: ./.github/workflows/service-discovery.yml
    secrets: inherit

  crawl:
    needs: discovery
    uses: ./.github/workflows/docs-crawler.yml
    secrets: inherit
