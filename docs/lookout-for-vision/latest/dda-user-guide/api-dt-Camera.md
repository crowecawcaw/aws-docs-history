Defect Detection App is in preview release and is subject to change.

# Camera

Information about a discovered camera. To get a list of discovered cameras,
call [GET /cameras](api-get-cameras.md "api-get-cameras.md").

## cameraId

The ID of the camera. To get the ID for a camera, you call the `GET
 /cameras` operation to get the discovered cameras.

Type: String
