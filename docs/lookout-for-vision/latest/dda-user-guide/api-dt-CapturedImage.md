Defect Detection App is in preview release and is subject to change.

# CapturedImage

Information about a captured image. To get the 12 most recent images in a folder, call
[GET /captured-images](api-get-captured-images.md "api-get-captured-images.md").

## image

The base64 encoded image.

Type: String

## path

The path to the image.

Type: String
