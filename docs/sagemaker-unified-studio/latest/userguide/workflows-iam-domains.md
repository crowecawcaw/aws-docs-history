# Workflows in IAM-based domains

IAM-based domains provide workflow capabilities through federated role access, where existing IAM permissions apply when using Amazon SageMaker Unified Studio workflows. This domain type offers a new interface and user experience for workflow management.

Workflow capabilities in IAM-based domains may differ from IDC-based domains. Refer to your domain administrator for specific feature availability and access requirements.
