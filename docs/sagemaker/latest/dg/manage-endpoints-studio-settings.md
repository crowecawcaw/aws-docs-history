# View settings

On the **Settings** tab, you can view the endpoint’s associated
AWS IAM role, the AWS KMS key used for encryption (if applicable), the name of
your VPC, and the network isolation settings.
