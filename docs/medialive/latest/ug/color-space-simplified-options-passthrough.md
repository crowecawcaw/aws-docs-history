# Passing through the color

space

You can set up to _pass through_ the color space
in a MediaLive output. These rules apply:

- The color space can be a supported color space or an unsupported color space.
- You can set up to include or remove the color space metadata.
- You can set up to pass through the color space in some outputs and to convert it in
  other outputs.
