# Input settings—Network input

settings

The fields in the Network input settings section apply only to HLS inputs and
multicast inputs.

- For HLS inputs, all the fields except **Multicast input
  settings** apply.
- For multicast inputs, only the **Multicast input
  settings** applies.
