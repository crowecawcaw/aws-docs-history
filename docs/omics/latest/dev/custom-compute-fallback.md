

# Custom compute fallback
<a name="custom-compute-fallback"></a>

The Custom compute fallback page was renamed and moved to a different URL. The new page: [Advanced resource configuration](advanced-resource-configuration.md). Update your bookmarks accordingly.