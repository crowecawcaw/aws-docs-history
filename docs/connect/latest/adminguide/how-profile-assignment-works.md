

# How Customer Profiles processes key definitions
<a name="how-profile-assignment-works"></a>

When Customer Profiles ingests the custom object mappings, it processes the key definitions. The following diagram shows how it processes standard identifiers in key definitions to determine which profile to assign the object to. 

![Flowchart showing profile key check process for assigning or queuing objects.](http://docs.aws.amazon.com/connect/latest/adminguide/images/customer-profiles-template1.png)
