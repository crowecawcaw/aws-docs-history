# Ingest data with AWS IoT SiteWise APIs

Use AWS IoT SiteWise APIs to send timestamped industrial data to your assets' attribute
and measurement properties. The APIs accepts payload containing timestamp-quality-value
(TQV) structures.
