# Limitations

The following are limitations for the Datadog connector:

- Datadog doesn’t support either field based or record based partitioning.
- `from` is mandatory filter parameter for `Log Queries`
  entity.
- `from_to_date` and `query` are mandatory filter parameters for
  `Metrics Timeseries` entity.
