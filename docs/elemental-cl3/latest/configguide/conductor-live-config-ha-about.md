# High availability

(HA)

If you have set up a primary and a secondary AWS Elemental Conductor Live node in a redundancy group, you
must enable HA in order to set the nodes up as an active redundant pair.

Enabling HA is typically the very last step that you perform when you
configure a cluster. You enable HA in order to start working with the
cluster.
